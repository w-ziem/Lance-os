FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
# Source is mounted as volumes at runtime — not copied here
EXPOSE 3000
ENV CHOKIDAR_USEPOLLING=true
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
